# SQL Queries - Samples

This folder provides SQL queries demonstrating geospatial distance filtering, aggregation, and window functions such as running totals for analytics use cases.
Data is based on a drone delivery service, with orders at each launch site. 
